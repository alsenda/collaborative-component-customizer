import type { JSX } from "preact";

/**
 * Renders the initial placeholder UI for the web application.
 */
export function App(): JSX.Element {
  return <main className="p-4">Demo web app scaffold is ready.</main>;
}
